<h1>Candidates list</h1>

<ul>
  <% @candidates.each do |candidate| %>
    <li><%= link_to "#{candidate.name} (#{candidate.slug})", edit_candidate_path(candidate) %></li>
  <% end %>
</ul>

<%= link_to 'new', new_candidate_path %>
